# Loaded automatically by `vite build` (and Vercel). The production frontend
# talks to the live backend. Local `npm run dev` ignores this file and falls
# back to http://localhost:8081 (see src/lib/api.js).
VITE_API_BASE_URL=https://portfolio.sudodeveloper.com
